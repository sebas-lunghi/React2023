import React from 'react'

const Button = ({text, onClick}) => {
  return (
    <button onClick={onClick}>
        <p1>
            Haz ckick aqui  
        </p1>

    </button>
  )
}

export default Button